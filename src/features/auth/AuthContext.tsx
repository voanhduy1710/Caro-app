/** Stable public entry point for authentication context. */
export { AuthProvider, useAuth } from './AuthContextProvider';
export type { CreateAccountResult, ProfileSaveResult, UserProfile } from './AuthContextProvider';
