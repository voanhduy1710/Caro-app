/**
 * Public engine boundary.
 *
 * The implementation is being decomposed by concern behind this stable
 * module, so protocol consumers never need to track internal file moves.
 */
export * from './roomEngineImplementation';
